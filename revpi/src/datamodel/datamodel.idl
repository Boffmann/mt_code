module RevPiDDS {
  struct Tasks {
    long taskType;
  };
  #pragma keylist Tasks taskType

  struct Decision {
    long senderID;
    long decision;
  };
  #pragma keylist Decision senderID

  struct State {
    long timestamp;
    double speed;
  };
  #pragma keylist State timestamp
};
